name: credo_chocolatine
on:
  push:
    branches-ignore:
        - ga-ignore-
  pull_request:
    branches-ignore:
        - ga-ignore-
    env:
        MIRROR_URL: https://github.com/EpitechPromo2026/B-DOP-200-COT-2-1-chocolatine-olouwachegoun.badou.git
        EXECUTABLES: pushwap

  workflow_dispatch:
jobs:
 check_repository_cleanliness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      #- name: Unwanted file detected
      # run:  find . -type f \( -name "*.o" -o -name "*~" -o -name "#*" -o -name "*.a" -o -name "*.so" -o -name "*.gcda" -o "*.gcov" -o -name "tmp" \)

       Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          find . -type f  -name "*.o" -o -name "*~" -o -name "#*" -o -name "*.a" -o -name "*.so" -o -name "*.gcda" -o -name "*.gcov" -o -name "tmp"
